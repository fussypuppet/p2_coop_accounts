<h2>Shareholder Index</h2>
<% if (shareholdersList) { %>
    <table class="table sidebar">
        <tr>
            <th>
                Name
            </th>
            <th>
                Unit
            </th>
            <th>
                Balance
            </th>
        </tr>
        <% shareholdersList.forEach(shareholder => { %>
            <tr class="shareholderRow" onclick="<%= `window.location.href='/shareholders/${shareholder.id}?years=2'` %>">
                <td>
                    <p><%= shareholder.name %></p>
                    <% if (shareholder.name2){ %>
                        <p>& <%= shareholder.name2 %></p>
                    <% } %>
                </td>
                <td>
                    <p><%= shareholder.unit.number %></p>
                </td>
                <td>
                    <%= shareholder.balance %>
                </td>
            </tr>
        <% }) %>
    </table>
<% } %>