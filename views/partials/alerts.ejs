<% if (alerts.error) { %>
    <% alerts.error.forEach(function(message) { %>
        <div class="flash flash_error">
            <%= message %>
        </div>
    <% }); %>
<% } else if (alerts.success){ %>
    <% alerts.success.forEach(function(message) { %>
        <div class="flash flash_message">
            <%= message %>
        </div>
    <% }); %>
<% } %>